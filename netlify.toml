# Static site — no build step needed.
# Python runs on GitHub Actions only, not here.
[build]
  publish = "."
  command = ""

[[headers]]
  for = "/signals_payload.json"
  [headers.values]
    Cache-Control = "no-cache, no-store, must-revalidate"
    Access-Control-Allow-Origin = "*"
